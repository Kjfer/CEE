# Activa la capa de datos simulada. "true" = usar mocks; "false" = consumir la API real.
VITE_USE_MOCKS=true
# URL base de la API real (usada solo cuando VITE_USE_MOCKS=false).
VITE_API_URL=http://localhost:3000/api
VITE_APP_NAME=CEE-FIIS
